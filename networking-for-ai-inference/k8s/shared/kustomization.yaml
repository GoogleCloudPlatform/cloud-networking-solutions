apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Inference Gateway - Shared Resources
#
# Deploys common resources needed before the main deployment:
# - ComputeClasses for GPU node auto-provisioning
#
# Usage: kubectl apply -k kubernetes/inference-gateway

namespace: default

resources:
    - computeclasses.yaml
