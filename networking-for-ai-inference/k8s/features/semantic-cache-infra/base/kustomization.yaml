apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base resources for Apigee APIM Operator
# Creates namespace only - IAM is handled via Terraform with Workload Identity Federation
#
# NOTE: Environment-specific configuration is defined in the parent kustomization.yaml
# (kubernetes/apim/kustomization.yaml) via apim-global-config ConfigMap.

resources:
  - namespace.yaml
