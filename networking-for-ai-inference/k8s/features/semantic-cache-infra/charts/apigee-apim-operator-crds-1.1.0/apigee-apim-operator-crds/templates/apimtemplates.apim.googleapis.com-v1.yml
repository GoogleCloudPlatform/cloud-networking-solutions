# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "apimtemplates.apim.googleapis.com"
spec:
  group: "apim.googleapis.com"
  names:
    kind: "ApimTemplate"
    plural: "apimtemplates"
    shortNames:
    - "template"
    - "apimtemplate"
    singular: "apimtemplate"
  scope: "Namespaced"
  versions:
  - additionalPrinterColumns:
    - jsonPath: ".status.currentState"
      name: "State"
      priority: 0
      type: "string"
    - jsonPath: ".status.errorMessage"
      name: "errorMessage"
      priority: 0
      type: "string"
    name: "v1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: "Specification for the Apim Template rule"
            properties:
              apimTemplateRule:
                description: "Specifies the Apim Template Rule this should use for\
                  \ validation."
                properties:
                  group:
                    type: "string"
                  kind:
                    type: "string"
                  name:
                    type: "string"
                  namespace:
                    type: "string"
                type: "object"
              templates:
                description: "Specifies the different lists of policies that should\
                  \ be executed as part of the request"
                items:
                  properties:
                    flows:
                      description: "Specification for request, response and post response\
                        \ flows. \nContains 3 types of flows pre flow, post flow and\
                        \ conditional flow. \nEach flow contains list of policies."
                      items:
                        properties:
                          condition:
                            description: "Specifies the condition to decide whether\
                              \ to execute this or not."
                            type: "string"
                          name:
                            description: "Specifies the name of the flow. \nFor Pre\
                              \ Flow it must be preflow and this cannot have a condition\n\
                              For post flow it must postflow and this cannot have\
                              \ a condition\nfor conditional it can be any other name."
                            type: "string"
                          policies:
                            description: "Specifies the ordered list of policies that\
                              \ should be executed as part of the request or response"
                            items:
                              properties:
                                condition:
                                  description: "Condition for executing the parameter"
                                  type: "string"
                                group:
                                  type: "string"
                                kind:
                                  type: "string"
                                name:
                                  type: "string"
                                namespace:
                                  type: "string"
                              type: "object"
                            type: "array"
                        type: "object"
                      type: "array"
                    mode:
                      description: "Specification for the mode of the flow. i.e. request,\
                        \ response and post response flows"
                      enum:
                      - "POST_RESPONSE"
                      - "REQUEST"
                      - "RESPONSE"
                      type: "string"
                  type: "object"
                type: "array"
            required:
            - "apimTemplateRule"
            type: "object"
          status:
            properties:
              currentState:
                enum:
                - "CREATED"
                - "CREATE_UPDATE_FAILED"
                - "CREATING"
                - "DELETE_FAILED"
                - "DELETING"
                - "RUNNING"
                - "UPDATING"
                type: "string"
              errorMessage:
                type: "string"
              operationResult:
                type: "string"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
