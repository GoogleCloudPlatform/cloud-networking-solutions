# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "apimtemplaterules.apim.googleapis.com"
spec:
  group: "apim.googleapis.com"
  names:
    kind: "ApimTemplateRule"
    plural: "apimtemplaterules"
    shortNames:
    - "apim-rule"
    - "apimrule"
    singular: "apimtemplaterule"
  scope: "Namespaced"
  versions:
  - additionalPrinterColumns:
    - jsonPath: ".status.currentState"
      name: "State"
      priority: 0
      type: "string"
    - jsonPath: ".status.errorMessage"
      name: "errorMessage"
      priority: 0
      type: "string"
    name: "v1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: "Specification for the Apim Template rule"
            properties:
              allowList:
                description: "list of policies that can be present in the ApimTemplate\
                  \ but are not necessary"
                items:
                  type: "string"
                type: "array"
              denyList:
                description: "list of denied policies that should not be present in\
                  \ the ApimTemplate"
                items:
                  type: "string"
                type: "array"
              override:
                description: "overrides the updates to the rule in case apim templates\
                  \ using the rule exists."
                type: "boolean"
              requiredList:
                description: "list of required policies that must be present in the\
                  \ ApimTemplate"
                items:
                  type: "string"
                type: "array"
            type: "object"
          status:
            properties:
              currentState:
                enum:
                - "CREATED"
                - "CREATE_UPDATE_FAILED"
                - "CREATING"
                - "DELETE_FAILED"
                - "DELETING"
                - "RUNNING"
                - "UPDATING"
                type: "string"
              errorMessage:
                type: "string"
              operationResult:
                type: "string"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
